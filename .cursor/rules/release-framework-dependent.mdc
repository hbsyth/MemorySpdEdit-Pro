---
description: 发布新版本两步：先同步源码，再发不包含依赖的单一文件
alwaysApply: true
---

# 发布约定（MemorySpdEdit-Pro）

当用户说「发布」「发布新版本」「Release」或同类表述时，**必须严格按此顺序**：

## 第一步：向 GitHub 同步源代码

1. 按 `Version.props` 规则升版本（同年周流水 +1，跨周从 `0001`）。
2. 提交全部相关源码变更（含 `Core/`、表单、脚本、规则等）。
3. 推送到远程 `pro`（仓库 `hbsyth/MemorySpdEdit-Pro`）。
4. **源码推送成功后**，才能进入第二步。

## 第二步：发布不包含依赖的单一文件

1. `dotnet publish` 使用 `--self-contained false` + `PublishSingleFile=true`（win-x64）。
2. **禁止**自包含（禁止 `--self-contained true` / 把 .NET 运行时打进包）。
3. 打 ZIP（一个 exe）→ 创建 GitHub Release。
4. 用户机器需已安装 .NET 10 Desktop Runtime x64。

## 流水线

使用 `scripts/Publish-Release.ps1`（内部：先 git commit/push，再调用 `launcher/publish-minimal.ps1` 打包并 `gh release create`）。

Release 说明与 README 须写明：**框架依赖单一文件 / 包内不含运行时依赖**。

「不包含依赖」= 发布包不内嵌运行时；**不要**理解成自包含（无依赖安装）。
