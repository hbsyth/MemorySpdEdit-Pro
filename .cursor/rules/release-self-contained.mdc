---
description: 发布新版本必须使用无依赖（自包含）单一文件打包
alwaysApply: true
---

# 发布约定（MemorySpdEdit-Pro）

当用户说「发布」「发布新版本」「Release」或同类表述时：

1. **产物必须是无依赖单一文件**：`dotnet publish` 使用 `--self-contained true` + `PublishSingleFile=true`（win-x64），**禁止**框架依赖（FDD / `--self-contained false`）发布包。
2. 走现有流水线：`scripts/Publish-Release.ps1`（内部调用 `launcher/publish-minimal.ps1`），按 `Version.props` 规则升版本 → 构建 → ZIP → 推送 `pro` 远程 → GitHub Release（仓库 `hbsyth/MemorySpdEdit-Pro`）。
3. Release 说明与 README 下载指引须写明：**自包含 / 无需安装 .NET 运行时**。
4. 不要改用需要用户单独安装 Desktop Runtime 的打包方式，除非用户明确要求。
